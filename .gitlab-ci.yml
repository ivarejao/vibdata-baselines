default:
    image: python:3.10
    before_script:
       - pip install -U pip
       - pip install poetry 
stages:
    - formatting

formatting:
    stage: formatting
    script:
        - poetry run isort --check-only vibdata/ tests/
        - poetry run black --check vibdata/ tests/
    only:
        - merge_requests
